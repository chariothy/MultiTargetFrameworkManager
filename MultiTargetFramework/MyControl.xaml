<UserControl x:Class="Nu.MultiTargetFramework.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Nu.MultiTargetFramework"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow">
    <Grid x:Name="MainGrid">
        <StackPanel Orientation="Vertical">
            <TextBlock Margin="10" HorizontalAlignment="Center">Multiple Target Framework</TextBlock>
            <Button Content="_Click Me!" Click="button1_Click" Width="120" Height="80" Name="button1"/>
            <TextBlock HorizontalAlignment="Left" Height="166" Margin="10,0,0,0" TextWrapping="Wrap" Text="{Binding Path=Info}" Width="280"/>
        </StackPanel>
    </Grid>
</UserControl>
